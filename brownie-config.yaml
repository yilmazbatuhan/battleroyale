dependencies:
  # - <organization/repo>@<version>
  - smartcontractkit/chainlink-brownie-contracts@0.2.2
compiler:
  solc:
    remappings:
      - '@chainlink=smartcontractkit/chainlink-brownie-contracts@0.2.2'
dotenv : .env
networks:
  rinkeby:  
    eth_usd_price_feed_address: 'address'
wallets:
  from_key : ${PRIVATE_KEY}
  